import { redirect } from 'next/navigation';

/**
 * The success cases shown here were made up: Gastrux has not run in a real restaurant yet (site claims
 * review, 2026-09-25). Old links land on how implementation works instead.
 */
export default function CasosDeSucesso() {
  redirect('/#como-comecar');
}
